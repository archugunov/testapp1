{
  "name": "testapp1",
  "private": true,
  "scripts": {
    "build": "esbuild app/javascript/*.* --bundle --outdir=app/assets/builds",
    "build:css": "bunx tailwindcss -i ./app/assets/stylesheets/application.tailwind.css -o ./app/assets/builds/application.css"
  },
  "devDependencies": {
    "esbuild": "^...",
    "@hotwired/turbo-rails": "^...",
    "@hotwired/stimulus": "^...",
    "tailwindcss": "^..."
  }
}